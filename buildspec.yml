version: 0.2

phases:
#  install:
#    runtime-versions:
#      java: corretto11
  pre_build:
  #    commands:
  #      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew build
      - ./gradlew sonar "-Dsonar.token=sqp_cfead217877a2d5bc79dbb0d7e831c21b6a22bd9 -Dsonar.qualitygate.wait=true"
  post_build:
#    commands:
#      - echo Build completed on date
artifacts:
  files:
    - build/libs/module4-thin.war
